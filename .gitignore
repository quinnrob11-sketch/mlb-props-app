node_modules
dist
*.local
.env*.local
.vercel
*.png

# Kalshi bot: credentials, per-machine config and trade journals never leave this PC
bot/config.json
bot/state/
bot/STOP
*.pem
*.key
.backtest-cache*
.kalshi-cache
scratch/
# agent/edge-pitch research scratch (caches, fitted params, run outputs)
.work/
# Study caches: public API responses, hundreds of MB, refetchable
ecache/
scratchtmp/